export const commands: Record<string, { action: string }> = {
  '!ping': { action: 'ping' },
  '!commands': { action: 'commands' },
  '!interesting': { action: 'interesting' },

  '!rolld6': { action: 'rolld6' },
  '!rolld20': { action: 'rolld20' },
  '!getUser': { action: 'getUser' },
}
